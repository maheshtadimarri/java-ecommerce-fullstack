import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';const API='http://localhost:8081/api';function App(){const[p,setP]=useState([]),[q,setQ]=useState(''),[cart,setCart]=useState([]);const load=()=>fetch(`${API}/products?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(setP);useEffect(load,[]);const checkout=()=>{if(!cart.length)return;fetch(`${API}/orders`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customerEmail:'customer@example.com',total:cart.reduce((s,x)=>s+x.price,0)})}).then(r=>r.ok&&alert('Order placed!'));setCart([])};return <main><header><h1>ShopSphere</h1><p>Modern commerce demo</p></header><div className="search"><input value={q}onChange={e=>setQ(e.target.value)}placeholder="Search products"/><button onClick={load}>Search</button></div><div className="grid">{p.map(x=><article key={x.id}><h2>{x.name}</h2><span>{x.category}</span><p>{x.description}</p><strong>${x.price.toFixed(2)}</strong><p>Stock: {x.stock}</p><button disabled={!x.stock}onClick={()=>setCart([...cart,x])}>Add to cart</button></article>)}</div><aside><h2>Cart ({cart.length})</h2><p>Total: ${cart.reduce((s,x)=>s+x.price,0).toFixed(2)}</p><button onClick={checkout}>Checkout</button></aside></main>}createRoot(document.getElementById('root')).render(<App/>);
